import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: false, // Temporarily disable strict mode to debug duplicate rendering issues
};

export default nextConfig;
